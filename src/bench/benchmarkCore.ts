import { HeuristicAI, type AiChoice } from "../ai/heuristic";
import { LookaheadAI } from "../ai/lookahead";
import { estimateSpinPotential } from "../ai/spinPotential";
import { executeBenchmarkAction } from "../ai/benchmarkRunner";
import { type WebPolicyJson, WebPolicyAI } from "../ai/webPolicy";
import { type WebValueJson, WebValueModel } from "../ai/webValue";
import { boardMetrics, TetrisEngine } from "../engine/tetris";

export type AiLike = { choose(engine: TetrisEngine): AiChoice | null };
export type Aggregate = { games:number; rounds:number; piecesSurvived:number; linesCleared:number; attackSent:number; topoutCount:number; avgHoles:number; avgMaxHeight:number; avgBumpiness:number; avgTotalHeight:number; tspinCount:number; tsdCount:number; tstCount:number; spinPotentialCreated:number; garbageHandled:number; avgDecisionTimeMs:number; maxDecisionTimeMs:number; spinFinisherAttempts:number; spinFinisherSuccesses:number; routeFailures:number; directPlacements:number; routedPlacements:number; spinFinisherRejectReasons:Record<string, number>; routeFailureReasons:Record<string, number>; routedNoSpin:number; routedNoClear:number; tPreserveActions:number; wastedTPlacements:number; slotDestroyedCount:number };
export type BenchEntry = { name:string; ai:AiLike };
export type BenchConfig = { games:number; maxPieces:number; seedBase:number };
export type BenchPayload = { generatedAt:string; environment:"browser"; games:number; maxPieces:number; seedBase:number; aiCount:number; results:Record<string, Aggregate>; worker?:boolean; canceled?:boolean; elapsedMs?:number };
export type ProgressEvent = { type:"started"|"ai_started"|"game_progress"|"ai_finished"|"finished"|"error"; message?:string; aiName?:string; game?:number; games?:number };

export const fmt = (n:number,d=2)=> Number.isFinite(n)?n.toFixed(d):"0";
export function renderSummary(payload: BenchPayload): string { const rows=Object.entries(payload.results).map(([name,a])=>[name.padEnd(13),`pieces ${String(a.piecesSurvived).padStart(5)}`,`topout ${String(a.topoutCount).padStart(3)}`,`atk ${String(a.attackSent).padStart(5)}`,`holes ${fmt(a.avgHoles,2).padStart(6)}`,`h ${fmt(a.avgMaxHeight,2).padStart(5)}`,`bump ${fmt(a.avgBumpiness,2).padStart(6)}`,`tsd ${String(a.tsdCount).padStart(3)}`,`tst ${String(a.tstCount).padStart(3)}`,`ms ${fmt(a.avgDecisionTimeMs,2).padStart(6)}`,`fin ${String(a.spinFinisherAttempts).padStart(3)}/${String(a.routedPlacements).padStart(3)}`].join(" | ")).join("\n"); return [`TetraFlux Browser AI Benchmark`,`generated: ${payload.generatedAt}`,`games=${payload.games} maxPieces=${payload.maxPieces} seed=${payload.seedBase} ai=${payload.aiCount}`,"",rows].join("\n"); }

const fetchJson = async <T>(u:string): Promise<T|null> => { try { const r=await fetch(`${u}${u.includes("?")?"&":"?"}t=${Date.now()}`,{cache:"no-store"}); if(!r.ok) return null; return await r.json() as T; } catch { return null; } };
export async function buildBrowserAis(): Promise<BenchEntry[]> { const ais=[{name:"HeuristicAI",ai:new HeuristicAI()},{name:"LookaheadAI",ai:new LookaheadAI({ depth:3, beamWidth:50, includeHold:true, spinBias:1, maxCandidatesPerNode:36, maxNodesPerDepth:300, timeBudgetMs:9 })}] as BenchEntry[]; const spinAi=new LookaheadAI({ depth:1, beamWidth:24, includeHold:true, spinBias:1.2, maxCandidatesPerNode:16, maxNodesPerDepth:120, timeBudgetMs:2.8 }); Object.assign(spinAi,{holeWeight:10.1,heightWeight:0.88,maxHeightWeight:2.35,bumpWeight:0.55,wellWeight:0.04,lineBonus:3.7,attackBonus:4.7,spinPotentialBonus:2.95,holdPenalty:0.01}); ais.push({name:"SpinAI",ai:spinAi}); const [p,v]=await Promise.all([fetchJson<WebPolicyJson>("/models/web_policy.json"),fetchJson<WebValueJson>("/models/web_value.json")]); const policy=p?.format==="tetraflux_web_policy_json_v1"?new WebPolicyAI(p):null; const value=v?.format==="tetraflux_web_value_json_v1"?new WebValueModel(v):null; if(policy){ ais.push({name:"WebPolicyAI",ai:policy}); if(value){const hybrid=new WebPolicyAI(policy.model); hybrid.setValueModel(value); ais.push({name:"HybridAI",ai:hybrid}); } } return ais; }

export async function runOneAi(entry: BenchEntry,cfg:BenchConfig,isCanceled:()=>boolean,onProgress?:(e:ProgressEvent)=>void): Promise<Aggregate> { let piecesSurvived=0,linesCleared=0,attackSent=0,topoutCount=0,tspinCount=0,tsdCount=0,tstCount=0,holesSum=0,maxHeightSum=0,bumpinessSum=0,totalHeightSum=0,spinPotentialCreated=0,garbageHandled=0,decisionMsTotal=0,decisionMsMax=0,decisions=0,spinFinisherAttempts=0,spinFinisherSuccesses=0,routeFailures=0,routedNoSpin=0,routedNoClear=0,directPlacements=0,routedPlacements=0,tPreserveActions=0,wastedTPlacements=0,slotDestroyedCount=0; const spinFinisherRejectReasons:Record<string,number>={},routeFailureReasons:Record<string,number>={}; for(let g=0; g<cfg.games; g++){ if(isCanceled()) throw new Error("Benchmark canceled"); const seed=cfg.seedBase+g*31; const engine=new TetrisEngine(seed,seed+17); for(let p=0; p<cfg.maxPieces && !engine.dead; p++){ if(isCanceled()) throw new Error("Benchmark canceled"); const beforePending=engine.pendingGarbage; const beforeState=engine.stateDict(); const t0=performance.now(); const action=entry.ai.choose(engine); const dt=performance.now()-t0; decisionMsTotal+=dt; decisionMsMax=Math.max(decisionMsMax,dt); decisions++; if(!action){topoutCount++;break;} const reason=String(((action.aiInfo??{}) as Record<string, unknown>).spinFinisherRejected??""); if(reason) spinFinisherRejectReasons[reason]=(spinFinisherRejectReasons[reason]??0)+1; const execution=executeBenchmarkAction(engine,action); const result=execution.result; if(execution.metrics.spinFinisherAttempt) spinFinisherAttempts++; if(execution.metrics.spinFinisherSuccess) spinFinisherSuccesses++; if(execution.metrics.routeFailed) routeFailures++; if(execution.metrics.routedNoSpin) routedNoSpin++; if(execution.metrics.routedNoClear) routedNoClear++; if(execution.metrics.routeFailureReason) routeFailureReasons[execution.metrics.routeFailureReason]=(routeFailureReasons[execution.metrics.routeFailureReason]??0)+1; if(execution.metrics.routeUsed) routedPlacements++; if(execution.metrics.usedDirectApply) directPlacements++; if(execution.metrics.tPreserveAction) tPreserveActions++; if(execution.metrics.wastedTPlacement) wastedTPlacements++; if(execution.metrics.slotDestroyed) slotDestroyedCount++; if(!result.ok){topoutCount++;break;} piecesSurvived++; linesCleared+=result.linesCleared; attackSent+=result.attackSent; if(result.topout||engine.dead) topoutCount++; if(result.spin==="tspin"){tspinCount++; if(result.linesCleared===2) tsdCount++; if(result.linesCleared===3) tstCount++;} spinPotentialCreated += estimateSpinPotential(beforeState).bonus; const afterPending=engine.pendingGarbage; const canceled=Math.max(0,beforePending-afterPending); garbageHandled += canceled + Math.min(result.attackSent,beforePending); const m=boardMetrics(engine.stateDict().board); holesSum+=m.holes; maxHeightSum+=m.maxHeight; bumpinessSum+=m.bumpiness; totalHeightSum+=m.totalHeight; } onProgress?.({type:"game_progress",aiName:entry.name,game:g+1,games:cfg.games,message:`${entry.name}: game ${g+1}/${cfg.games}`}); } const rounds=Math.max(1,piecesSurvived); return {games:cfg.games,rounds,piecesSurvived,linesCleared,attackSent,topoutCount,avgHoles:holesSum/rounds,avgMaxHeight:maxHeightSum/rounds,avgBumpiness:bumpinessSum/rounds,avgTotalHeight:totalHeightSum/rounds,tspinCount,tsdCount,tstCount,spinPotentialCreated,garbageHandled,avgDecisionTimeMs:decisions?decisionMsTotal/decisions:0,maxDecisionTimeMs:decisionMsMax,spinFinisherAttempts,spinFinisherSuccesses,routeFailures,directPlacements,routedPlacements,spinFinisherRejectReasons,routeFailureReasons,routedNoSpin,routedNoClear,tPreserveActions,wastedTPlacements,slotDestroyedCount}; }
