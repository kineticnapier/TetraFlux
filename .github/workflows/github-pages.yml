name: GitHub Pages disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Pages disabled
        run: |
          echo "GitHub Pages deployment is disabled."
          echo "This project deploys to Cloudflare Pages with .github/workflows/deploy-cloudflare.yml."
